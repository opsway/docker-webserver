Docker webserver containers
---------------------------

- NGINX with placeholders (You can set it via docker ENV):
    * DOMAIN
    * APPLICATION_ENV
    * HTTP_AUTH (string from htpasswd file)
- PHP-FPM
- POSTGRESQL
